version: '3.8'

services:
  # frontend:
  #   container_name: frontend
  #   build:
  #     context: ./frontend
  #     dockerfile: Dockerfile
  #   restart: always
  #   network_mode: host
  #   ports:
  #     - '4200:4200'

  backend:
    container_name: backend
    build:
      context: ./backend
      dockerfile: Dockerfile
    restart: always
    network_mode: host
    ports:
      - '8080:8080'
    env_file:
      - ./backend/.env

  bot:
    container_name: bot
    build:
      context: ./kaiteki-bot
      dockerfile: Dockerfile
    restart: always
    network_mode: host
    env_file:
      - ./kaiteki-bot/.env
